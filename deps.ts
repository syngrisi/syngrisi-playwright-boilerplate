// workaround for https://github.com/microsoft/playwright/issues/17075 look 'npm run pretest' for compile
// now you can import packages as `import ora from '../deps.bundle'`
import ora from "ora";

export { ora };
